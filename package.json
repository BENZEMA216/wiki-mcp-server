{
  "name": "wiki-mcp-server",
  "version": "0.1.0",
  "description": "MCP Server that exposes an LLM-compiled knowledge base as queryable Agent tools",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "glob": "^11.0.0"
  }
}
