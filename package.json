{
  "name": "wiki-mcp-server",
  "version": "0.2.0",
  "description": "Remote MCP Server — turn an LLM-compiled knowledge base into a queryable Agent node accessible over HTTP/SSE",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:stdio": "node server-stdio.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "glob": "^11.0.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  }
}
